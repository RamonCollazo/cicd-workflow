name: Study Tracker Web Tests

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check_paths:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.filter.outputs.web }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4.0.1
        id: filter
        with:
          filters: |
            web:
              - './apps/study-tracker/web/**'
              - '.github/workflows/study-tracker-web-tests.yaml'
              - '.github/workflows/_test-python-app.yaml'

  test:
    name: Test Web
    needs: check_paths
    if: needs.check_paths.outputs.should_run == 'true'
    uses: ./.github/workflows/_test-python-app.yaml
    with:
      app_name: web
      working_directory: ./apps/study-tracker/web/
      image_name: study-tracker-web
    permissions:
      contents: read
      pull-requests: write

  tests-required:
    name: tests-required
    needs: [check_paths, test]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Aggregate result
        run: |
          if [[ "${{ needs.check_paths.outputs.should_run }}" != "true" ]]; then
            echo "Paths unchanged for web; passing through."
            exit 0
          fi
          if [[ "${{ needs.test.result }}" == "success" ]]; then
            echo "Web tests passed."
            exit 0
          fi
          echo "Web tests failed or were cancelled: ${{ needs.test.result }}"
          exit 1
