# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# VCS
.git/
.gitignore

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Docker
Dockerfile
.dockerignore

# Local runtime data — must never be baked into the image.
# The container persists state via the /data volume instead.
data/
